#!/bin/sh

set -e

if [ ! -f /etc/default/esplash ]; then
    ln -s /usr/share/esplash-data/v3.cfg /etc/default/esplash
fi
if [ ! -f /etc/default/fb-clean ]; then
    ln -s /usr/share/esplash-data/v3-shutdown.cfg /etc/default/fb-clean
fi

#DEBHELPER#

exit 0
